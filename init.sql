CREATE TABLE users(id serial primary key, login varchar(100), password varchar(100));

CREATE TABLE results(id serial primary key, x varchar(100), y varchar(100), r varchar(100), value varchar(100), exec_time varchar(100), time varchar(100));

